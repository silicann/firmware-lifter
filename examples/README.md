# Examples

Each transport subdirectory contains its transport-specific configuration file:

- `.firmware-lifter-transport.yaml`

The shared profile examples live at `examples/.firmware-lifter-profiles.yaml`.

Available transport examples:

- `custom/`
- `gdb/`
- `openocd/`
- `stm32flash/`
